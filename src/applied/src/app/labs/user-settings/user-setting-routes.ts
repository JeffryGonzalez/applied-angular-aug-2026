import { Routes } from '@angular/router';
import { UserSettings } from './user-settings';

export const userSettingRoutes: Routes = [
  {
    path: '',
    component: UserSettings,
  },
];

export default userSettingRoutes;
